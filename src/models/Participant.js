class Participant {
    constructor(teamId, tournamentId, registrationDate) {
        this.teamId = teamId;
        this.tournamentId = tournamentId;
        this.registrationDate = registrationDate;
    }
}